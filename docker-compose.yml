version: '3'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - bundle_rails4_2_11_3:/usr/local/bundle
    tty: true
  db:
    image: mysql:5.7.36
    volumes:
      - mysqldb:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD

volumes:
  mysqldb:
    driver: local
  bundle_rails4_2_11_3:
    driver: local
